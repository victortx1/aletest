<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>FPS Engenharia</title>
  <style>
    :root{
      --accent:#8a2be2;      /* roxo/accent */
      --accent-2:#5b2a86;
      --glass: rgba(255,255,255,0.06);
      --white-80: rgba(255,255,255,0.9);
      --shadow: 0 18px 30px rgba(0,0,0,0.45);
      --radius:14px;
      --max-width:1100px;
      font-family: Inter, "Segoe UI", Roboto, Arial, sans-serif;
    }

    *{box-sizing:border-box;margin:0;padding:0}
    body{
      min-height:100vh;
      background:#f3e0f8;
      color:#fff;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }

    /* Container to center the "site mock framed" like the image you sent */
    .frame {
      max-width: var(--max-width);
      margin:60px auto;
      border-radius:18px;
      overflow:hidden;
      box-shadow: var(--shadow);
      border:6px solid rgba(255,255,255,0.9);
      background:linear-gradient(180deg, rgba(0,0,0,0.35), rgba(0,0,0,0.55));
    }

    header{
      position:relative;
      height:520px; /* altura da hero */
      display:flex;
      align-items:flex-start;
      justify-content:center;
      overflow:hidden;
      background:#000;
    }

    /* video / background */
    .bg-media{
      position:absolute;
      inset:0;
      width:100%;
      height:100%;
      object-fit:cover;
      filter: saturate(0.9) contrast(0.9) brightness(0.5);
      transform:scale(1.02);
      z-index:0;
    }

    /* dark gradient overlay to ensure readability */
    .overlay{
      position:absolute; inset:0;
      background: linear-gradient(180deg, rgba(15,7,30,0.55) 0%, rgba(8,2,20,0.9) 80%);
      z-index:1;
    }

    /* Top nav */
    .nav{
      position:relative;
      z-index:3;
      width:100%;
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding:22px 36px;
      gap:12px;
    }

    .nav .left,
    .nav .right{
      display:flex;
      align-items:center;
      gap:18px;
    }

    .nav a{
      color:rgba(255,255,255,0.85);
      text-decoration:none;
      font-weight:600;
      letter-spacing:0.6px;
      font-size:14px;
      opacity:0.9;
    }

    /* centered logo in the header like the reference */
    .nav .logo{
      position:absolute;
      left:50%;
      transform:translateX(-50%);
      z-index:4;
      display:flex;
      align-items:center;
      gap:10px;
      pointer-events:none; /* não atrapalha clique no menu */
    }
    .logo img{ width:78px; height:78px; object-fit:contain; border-radius:12px; background:linear-gradient(180deg,var(--accent),var(--accent-2)); padding:8px; box-shadow:0 8px 20px rgba(0,0,0,0.6)}
    .logo h1{ font-size:18px; font-weight:800; color:var(--white-80); letter-spacing:1px; margin-left:4px; pointer-events:auto; }

    /* Hero content bottom */
    .hero-content{
      position:relative;
      z-index:4;
      margin-top:auto;
      width:100%;
      padding:28px 46px;
      display:flex;
      justify-content:space-between;
      align-items:flex-end;
      gap:20px;
    }

    .hero-left{
      max-width:60%;
      color:var(--white-80);
      text-shadow: 0 6px 20px rgba(0,0,0,0.6);
    }

    .hero-left h2{
      font-size:30px;
      margin-bottom:10px;
      font-weight:800;
    }
    .hero-left p{
      opacity:0.95;
      font-size:14px;
    }

    /* contact box bottom right (small) */
    .contact-card{
      background: linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.02));
      padding:14px 18px;
      border-radius:12px;
      backdrop-filter: blur(6px);
      text-align:right;
      min-width:230px;
      box-shadow: 0 8px 30px rgba(0,0,0,0.45);
    }
    .contact-card small{ display:block; font-size:12px; color:rgba(255,255,255,0.75); margin-bottom:6px }
    .contact-card a{
      display:inline-block;
      font-weight:700;
      text-decoration:none;
      color:#fff;
      background:linear-gradient(90deg,var(--accent),var(--accent-2));
      padding:8px 12px;
      border-radius:8px;
    }

    /* Main sections */
    main{ padding:44px 20px; background:linear-gradient(180deg,#f3e0f8 0%, #f6e9fb 30%); color:#222; }
    .container{ max-width:var(--max-width); margin:0 auto; padding:0 18px; }

    .services{
      display:grid;
      grid-template-columns:repeat(3,1fr);
      gap:18px;
      margin-top:20px;
    }
    .card{
      border-radius:12px;
      padding:20px;
      background:#fff;
      box-shadow: 0 10px 20px rgba(36,24,60,0.06);
    }
    .card h3{ color:var(--accent-2); margin-bottom:8px }
    .card p{ font-size:14px; color:#444 }

    /* portfólio grid */
    .portfolio{
      margin-top:28px;
      display:grid;
      grid-template-columns:repeat(3,1fr);
      gap:12px;
    }
    .portfolio img{ width:100%; height:160px; object-fit:cover; border-radius:10px }

    footer{
      margin-top:36px;
      padding:26px 0;
      text-align:center;
      color:#555;
      font-size:14px;
    }

    /* responsive */
    @media (max-width:980px){
      .services, .portfolio{ grid-template-columns:repeat(2,1fr) }
      .hero-left{ max-width:65% }
      .nav a{ display:none } /* mobile: hide normal menu to mimic hamburger; but we keep logo */
    }
    @media (max-width:640px){
      .frame{ margin:20px; }
      header{ height:520px }
      .hero-left h2{ font-size:22px }
      .hero-content{ flex-direction:column; align-items:flex-start; gap:12px }
      .contact-card{ width:100%; text-align:left; min-width:unset }
      .services, .portfolio{ grid-template-columns:1fr }
    }

  </style>
</head>
<body>

  <div class="frame" role="document">
    <header>
      <!-- Replace src with a mp4 or a large image. If you want a video like the tiktok, use <video autoplay muted loop> -->
      <video class="bg-media" autoplay muted loop playsinline>
        <source src="your-video.mp4" type="video/mp4">
        <!-- fallback image -->
      </video>

      <div class="overlay"></div>

      <nav class="nav" aria-label="Menu principal">
        <div class="left">
          <a href="#about">About</a>
          <a href="#services">Services</a>
        </div>

        <div class="logo" aria-hidden="false">
          <!-- troque pela logo da FPS -->
          <img src="logo-placeholder.png" alt="Logo FPS Engenharia">
          <h1>FPS Engenharia</h1>
        </div>

        <div class="right">
          <a href="#jobs">Jobs</a>
          <a href="#contact">Contact</a>
        </div>
      </nav>

      <div class="hero-content">
        <div class="hero-left">
          <h2>Transformando projetos elétricos e automação com qualidade</h2>
          <p>Projetos residenciais e comerciais • Instalações elétricas • Automação predial • Manutenção preventiva</p>
        </div>

        <div class="contact-card" id="contact">
          <small>Para orçamentos rápidos:</small>
          <div>
            <a href="https://wa.me/55SEUNUMERO?text=Ol%C3%A1%20FPS%20Engenharia%2C%20quero%20um%20or%C3%A7amento" target="_blank" rel="noopener">Falar no WhatsApp</a>
          </div>
          <div style="margin-top:8px; font-size:13px; color:rgba(255,255,255,0.8)">
            <div>Tel: (xx) xxxx-xxxx</div>
            <div>Email: contato@fpsengenharia.com</div>
          </div>
        </div>
      </div>
    </header>

    <main class="container">
      <section id="services">
        <h2 style="color:#222">Nossos serviços</h2>
        <div class="services">
          <div class="card">
            <h3>Projetos Elétricos</h3>
            <p>Elaboração de projetos residenciais, comerciais e industriais com normas ABNT.</p>
          </div>
          <div class="card">
            <h3>Instalações e Manutenção</h3>
            <p>Instalação de quadros, cabeamento, aterramento e manutenção preventiva.</p>
          </div>
          <div class="card">
            <h3>Automação Residencial</h3>
            <p>Automação de iluminação, persianas, controle remoto e integração com assistentes.</p>
          </div>
        </div>

        <h2 style="margin-top:26px; color:#222">Portfólio</h2>
        <div class="portfolio">
          <img src="thumb1.jpg" alt="Projeto 1">
          <img src="thumb2.jpg" alt="Projeto 2">
          <img src="thumb3.jpg" alt="Projeto 3">
        </div>
      </section>

      <footer>
        © <span id="year"></span> FPS Engenharia — Todos os direitos reservados
      </footer>
    </main>
  </div>

  <script>
    // small helper
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>
